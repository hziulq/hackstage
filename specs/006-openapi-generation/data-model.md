# Phase 1 Data Model: openapi.jsonの生成

該当なし。本featureは新規の永続化エンティティを追加せず、既存のAPIルーティングと既存の
marshmallow Schema(`RegisterSchema` / `LoginSchema` / `UserSchema` / `TodoSchema`)から
OpenAPIスキーマ(`openapi.json`)を生成するのみ。
